#include <bits/stdc++.h>

using namespace std;

const int N = 2e6 + 6;

int n, k;
char s[N][2];

int main() {
    ios::sync_with_stdio(false);

    cin >> n >> k;
    for (int i = 1; i <= n; ++i) cin >> s[i][0];
    for (int i = 1; i <= n; ++i) cin >> s[i][1];

    for (int i = 1; i < n; ++i) {
        
    }

}